# Maze

Tiny raylib maze generator and pathfinding visualizer.

## Usage

- Build the builder: `cc build.c -o build`
- Build the app: `./build`
- Run the App: `./maze`

Toggle algorithms by swapping the include in `maze.c` (see the commented options near the top).
